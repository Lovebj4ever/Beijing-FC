<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.myapplication">

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Search"></activity>
        <activity android:name=".Display" />
        <activity
            android:name=".MainActivity"
            android:label="Diary">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChartActivity"
            android:label="Chart" />
        <activity android:name=".datepick" />
        <activity android:name=".date_calculate" />
        <activity android:name=".NewEvent" />
        <activity android:name=".SpecialDay" />
        <activity android:name=".EditActivity"></activity>
        <activity android:name=".NotificationActivity"></activity>
        <activity android:name=".test_month" />
        <activity android:name=".monthlycost" />
        <activity android:name=".totalCost" />
        <activity android:name=".AccountBookActivity"></activity>
    </application>

</manifest>